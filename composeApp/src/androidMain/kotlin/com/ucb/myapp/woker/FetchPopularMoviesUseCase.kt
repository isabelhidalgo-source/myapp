package com.ucb.myapp.woker

class FetchPopularMoviesUseCase {
    operator fun invoke(): Result<List<Any>> {
        // Implementación simulada (Dummy)
        // Puedes reemplazar List<Any> por tu modelo de dominio real
        return Result.success(emptyList())
    }
}